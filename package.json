{
  "name": "restaurant-waitlist",
  "version": "1.0.0",
  "description": "Restaurant waiting list app",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
